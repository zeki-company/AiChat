package com.cdsy.aichat.util



/*
fun showUpdateDialog(
    context: Context,
    downloadUrl: String,
    content: String
) {
    AllenVersionChecker
        .getInstance()
        .downloadOnly(
            UIData.create()
                .setTitle("发现新版本")
                .setContent(content)
                .setDownloadUrl(downloadUrl)
        )
        .executeMission(context);
}*/
